# Profil développement local
spring:
  datasource:
    url: jdbc:mariadb://${DB_HOST:mariadb}:3306/${DB_NAME:air-sphere-connect}
    username: ${DB_USER:air-sphere-connect_user}
    password: ${DB_PASSWORD:userpass123}
    driver-class-name: org.mariadb.jdbc.Driver

  docker:
    compose:
      enabled: false

  messages:
    basename: ValidationMessages
    encoding: UTF-8

  security:
    user:
      name: admin
      password: admin123
      roles: ADMIN

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect

  mail:
    host: localhost
    port: 1025
    username: ""
    password: ""
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

# CORS
cors:
  allowed-origins: "http://localhost:4200"
  allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
  allowed-headers: "*"

#API
weather:
  api:
    key: e935d0947600dd8f7247eefe1819751a

mailjet:
  api:
    key: 13f60c773c9d6b968a6415fa88e6affc
    secret: 361d897cbe85bd88f3ddfea48599a081