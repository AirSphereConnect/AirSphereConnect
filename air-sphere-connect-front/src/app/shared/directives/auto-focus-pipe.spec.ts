import { AutoFocusPipe } from './auto-focus-pipe';

describe('AutoFocusPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoFocusPipe();
    expect(pipe).toBeTruthy();
  });
});
